#include<stdio.h>
#include<conio.h>
int main(){
int num,min=99,max=0,sum,i;
for(i=1;i<=5;i++){
printf("Enter the two digit number: ");
scanf("%d",&num);
if(num>max){
  max=num;
}if(num<min){
  min=num;
}}
printf("The large number is: %d\n",max);
printf("The small number is: %d",min);
return 0;
}
